## Express.js REST API
### Setup
1. Clone repo
2. Run `npm install`
3. Start server: `node server.js`
4. API endpoints:
   - `GET /items`
   - `GET /items/:id`
   - `POST /items`
   - `PUT /items/:id`
   - `DELETE /items/:id`
